FROM postgres:16

# Конфигурация БД (пользователь, пароль, имя базы)
# передаётся через переменные окружения при запуске контейнера,
# а не вшивается в образ.

COPY ini.sql /docker-entrypoint-initdb.d/ini.sql

